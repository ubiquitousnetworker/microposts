<section class="user_info">
  <h1>
    <%= gravatar_for @user %>
    <%= @user.name %>
  </h1>
  <%= link_to 'followings', followings_user_path(@user), class: "btn btn-primary btn-xs" %>
  <%= link_to 'followers', followers_user_path(@user), class: "btn btn-primary btn-xs" %>